// Pink Phase 9 — Autonomous Evolution, laboratory-first and production-protected.
(function(root,factory){const api=factory(root);if(typeof module==='object'&&module.exports)module.exports=api;if(typeof window!=='undefined')root.PinkAutonomousEvolution=api})(typeof window!=='undefined'?window:globalThis,function(root){
'use strict';
const clone=v=>v==null?v:JSON.parse(JSON.stringify(v));
const BLOCKED_ACTIONS=new Set(['merge_main','publish_production','delete_production_data','change_billing','change_credentials','weaken_security','permission_escalation']);
class CandidateStore{
 constructor(){this.items=new Map()}
 key(c){return `${c.kind||'improvement'}:${String(c.title||'').trim().toLowerCase()}`}
 add(input={}){if(!input.title)throw new Error('candidate_title_required');const key=this.key(input);const existing=this.items.get(key);if(existing){existing.hits+=1;existing.lastSeenAt=new Date().toISOString();existing.evidence=[...new Set([...(existing.evidence||[]),...(Array.isArray(input.evidence)?input.evidence:[input.evidence].filter(Boolean))])].slice(-10);if(input.status)existing.status=input.status;if(input.approvedForEvolution===true){existing.approvedForEvolution=true;existing.approvedAt=input.approvedAt||existing.approvedAt||new Date().toISOString();existing.approvedBy=input.approvedBy||existing.approvedBy||'Paulo Ricardo'}existing.score=this.score(existing);return clone(existing)}const item={id:input.id||`cand_${Date.now().toString(36)}_${Math.random().toString(36).slice(2,7)}`,key,kind:input.kind||'improvement',title:String(input.title),evidence:Array.isArray(input.evidence)?input.evidence:[input.evidence].filter(Boolean),impact:Number(input.impact??.5),confidence:Number(input.confidence??.5),risk:Number(input.risk??.3),effort:Number(input.effort??.4),hits:Number(input.hits||1),status:input.status||'candidate',approvedForEvolution:input.approvedForEvolution===true,approvedAt:input.approvedAt||null,approvedBy:input.approvedBy||null,createdAt:input.createdAt||new Date().toISOString(),lastSeenAt:input.lastSeenAt||new Date().toISOString()};item.score=this.score(item);this.items.set(key,item);return clone(item)}
 score(c){return Math.max(0,Math.min(1,(c.impact*.38+c.confidence*.27+Math.min(c.hits/5,1)*.15)-c.risk*.12-c.effort*.08))}
 approve(id,{approvedBy='Paulo Ricardo'}={}){const item=[...this.items.values()].find(c=>c.id===String(id));if(!item)throw new Error('evolution_candidate_not_found');item.status='approved_for_evolution';item.approvedForEvolution=true;item.approvedBy=approvedBy;item.approvedAt=new Date().toISOString();item.lastSeenAt=item.approvedAt;return clone(item)}
 list(){return [...this.items.values()].sort((a,b)=>b.score-a.score).map(clone)}
}
class EvolutionEngine{
 constructor(){this.candidates=new CandidateStore();this.experiments=[];this.learning=[]}
 observe(signal={}){const type=signal.type||'observation';if(['bug','security','reliability','performance','ux','provider_failure','workflow_friction','user_correction'].includes(type)){return this.candidates.add({id:signal.id,kind:type,title:signal.title||signal.message||type,evidence:signal.evidence||signal.message,impact:signal.impact??(type==='security'?.9:.65),confidence:signal.confidence??.7,risk:signal.risk??.25,effort:signal.effort??.4,hits:signal.hits,status:signal.status,approvedForEvolution:signal.approvedForEvolution===true,approvedAt:signal.approvedAt,approvedBy:signal.approvedBy,createdAt:signal.createdAt,lastSeenAt:signal.lastSeenAt})}return null}
 approve(id,{source='ui'}={}){
   let item=this.candidates.approve(id,{approvedBy:'Paulo Ricardo'});let persisted=false;
   try{const saved=root?.PinkEvolution?.approveCandidate?.(id,{source});if(saved){item={...item,...saved};persisted=true}}catch(error){if(typeof window!=='undefined')root?.PinkEvolution?.recordIssue?.('evolution-approval-persist',error?.message||error)}
   if(!persisted&&typeof window!=='undefined'&&root?.PinkMemoryCloud?.remember&&root?.PinkPublicConfig?.environment==='production'){
     const text=`APPROVED FOR EVOLUTION: ${item.title}\nKind: ${item.kind}\nStatus: approved_for_evolution\nEvidence: ${(item.evidence||[]).join(' · ')||'n/a'}\nApproved by: Paulo Ricardo\nApproved at: ${item.approvedAt}`;
     Promise.resolve(root.PinkMemoryCloud.remember({type:'evolution_signal',text,importance:1,explicit:true,source:'pink-runtime-evolution-approval',data:{candidateId:item.id,key:item.key,kind:item.kind,status:'approved_for_evolution',approvedForEvolution:true,approvedAt:item.approvedAt,approvedBy:'Paulo Ricardo',productionApproved:false}})).catch(()=>{});
   }
   this.recordLearning({candidateId:id,result:'human-approved-for-evolution',source});return clone(item)
 }
 prioritize(limit=10){return this.candidates.list().filter(c=>c.score>=.35&&c.approvedForEvolution===true).slice(0,limit)}
 can(action){return !BLOCKED_ACTIONS.has(String(action))}
 async experiment(candidate,adapters={}){if(!candidate?.id)throw new Error('candidate_required');if(candidate.approvedForEvolution!==true)return {id:null,candidateId:candidate.id,status:'needs_human_approval',branch:null,review:null,result:null,startedAt:null,endedAt:null};const exp={id:`exp_${Date.now().toString(36)}`,candidateId:candidate.id,status:'running',branch:null,review:null,result:null,startedAt:new Date().toISOString(),endedAt:null};this.experiments.push(exp);try{if(typeof adapters.createBranch!=='function'||typeof adapters.implement!=='function')throw Object.assign(new Error('evolution_lab_adapter_unavailable'),{code:'BLOCKED_EXTERNAL'});exp.branch=await adapters.createBranch(candidate);exp.result=await adapters.implement(candidate,exp.branch);if(typeof adapters.review==='function')exp.review=await adapters.review(candidate,exp.result);if(exp.review?.decision==='reject')exp.status='rejected_regression';else exp.status='needs_approval';exp.endedAt=new Date().toISOString();return clone(exp)}catch(error){exp.status=error?.code==='BLOCKED_EXTERNAL'?'blocked_external':'failed';exp.result={error:String(error?.message||error)};exp.endedAt=new Date().toISOString();return clone(exp)}}
 recordLearning(entry){this.learning.push({at:new Date().toISOString(),...clone(entry)});this.learning=this.learning.slice(-200)}
 snapshot(){return {version:'9.2.0',policy:'OBSERVE_PROPOSE_HUMAN_APPROVE_EVOLVE_NO_SELF_PUBLISH',blockedActions:[...BLOCKED_ACTIONS],candidates:this.candidates.list(),approvedCandidates:this.candidates.list().filter(c=>c.approvedForEvolution===true),experiments:clone(this.experiments),learning:clone(this.learning)}}
}
const engine=new EvolutionEngine();
function ingestLegacy(){if(typeof window==='undefined'||!window.PinkEvolution?.snapshot)return;try{const legacy=window.PinkEvolution.snapshot();for(const c of legacy.candidates||[])engine.observe({id:c.id,type:c.kind||'reliability',title:c.title,evidence:c.evidence,impact:c.priority==='high'?.8:.55,status:c.status,approvedForEvolution:c.approvedForEvolution===true,approvedAt:c.approvedAt,approvedBy:c.approvedBy,hits:c.hits,createdAt:c.createdAt,lastSeenAt:c.lastSeenAt})}catch(_){}}
if(typeof window!=='undefined'){
 ingestLegacy();
 window.addEventListener('pinkevolution:issue',e=>{const d=e.detail||{};engine.observe({type:/provider|research|brain|nvidia|gemini|openai/.test(String(d.type||''))?'provider_failure':'reliability',title:`Runtime: ${d.type||'issue'}`,message:d.message||d.evidence||'runtime issue',evidence:d.evidence||d.message,impact:.72,confidence:.9,risk:.18,effort:.35})});
 window.addEventListener('pinkevolution:feedback',e=>{const d=e.detail||{};if(d.signal==='negative')engine.observe({type:'user_correction',title:'Correção reportada pelo usuário',evidence:d.context||'feedback negativo',impact:.75,confidence:.95,risk:.15,effort:.35})});
 window.addEventListener('pinkevolution:candidate',e=>{const d=e.detail||{};if(d.title)engine.observe({id:d.id,type:d.kind||'reliability',title:d.title,evidence:d.evidence,impact:d.priority==='high'?.82:.62,confidence:.85,risk:.2,effort:.4,status:d.status,approvedForEvolution:d.approvedForEvolution===true,approvedAt:d.approvedAt,approvedBy:d.approvedBy,hits:d.hits,createdAt:d.createdAt,lastSeenAt:d.lastSeenAt})});
 window.addEventListener('pinkevolution:approved',e=>{const d=e.detail||{};if(!d.id)return;try{engine.candidates.approve(d.id,{approvedBy:d.approvedBy||'Paulo Ricardo'});engine.recordLearning({candidateId:d.id,result:'human-approved-for-evolution',source:'runtime-event'})}catch(_){}});
}
return {version:'9.2.0',engine,observe:s=>engine.observe(s),approve:(id,o)=>engine.approve(id,o),prioritize:n=>engine.prioritize(n),experiment:(c,a)=>engine.experiment(c,a),can:a=>engine.can(a),snapshot:()=>engine.snapshot()};
});
